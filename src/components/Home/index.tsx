import React from 'react';
import IndexInfo from '../IndexInfo';
import TopPerformingStocks from '../TopPerformingStocks';

const Home = () => {
  return (
    <div className="w-full h-full p-[1.6rem] overflow-x-hidden flex flex-col gap-[1.6rem]"></div>
  );
};

export default Home;
